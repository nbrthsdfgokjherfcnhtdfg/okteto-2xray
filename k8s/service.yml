apiVersion: v1
kind: Service
metadata:
  name: afvbhyjiklm
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "afvbhyjiklm"
    port: 8080
  selector:
    app: afvbhyjiklm
