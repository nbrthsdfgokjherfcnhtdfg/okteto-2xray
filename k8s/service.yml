apiVersion: v1
kind: Service
metadata:
  name: heriuom
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "heriuom"
    port: 8080
  selector:
    app: heriuom
