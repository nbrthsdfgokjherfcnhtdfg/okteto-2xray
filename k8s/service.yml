apiVersion: v1
kind: Service
metadata:
  name: liberary
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "liberary"
    port: 8080
  selector:
    app: liberary
