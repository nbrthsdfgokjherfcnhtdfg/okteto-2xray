apiVersion: v1
kind: Service
metadata:
  name: racexsgvn
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "racexsgvn"
    port: 8080
  selector:
    app: racexsgvn
